.imagelist
  %ul
    - users.each do |user|
      %li
        = link_to user_url(user, subdomain: false), title: user.name do
          = image_tag 'logo.png', alt: user.name, title: user.name, 'data-src' => user.image
